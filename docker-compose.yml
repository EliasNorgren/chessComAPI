services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./SQL/chess_games.db:/app/SQL/chess_games.db
    environment:
      - STOCKFISH_ENGINE_PATH=src/stockfish
      - FLASK_PORT=5000
    restart: unless-stopped
